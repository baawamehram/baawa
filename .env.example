# Server
# NODE_ENV=production  # Set automatically by Railway; set to 'production' locally to test CORS behaviour
DATABASE_URL=postgresql://user:pass@host:5432/baawa_mehram  # PostgreSQL connection string (must include pgvector extension)
RESEND_API_KEY=re_...                                        # Resend API key for sending emails
ANTHROPIC_API_KEY=sk-ant-...                                 # Anthropic API key for Claude (used in AI scoring/generation)
GOOGLE_AI_API_KEY=AIza...                                    # Google AI API key for embeddings (pgvector knowledge base) — get from aistudio.google.com
GROQ_API_KEY=gsk_...                                         # Groq API key for Whisper voice transcription — get from console.groq.com
FOUNDER_API_KEY=your-secret-key-here                         # Secret password to access the /dashboard route
PORT=3001                                                    # Port the Express server listens on
CLAUDE_MODEL=claude-haiku-4-5-20251001                       # Claude model ID used for assessment scoring
CLIENT_URL=https://your-vercel-app.vercel.app                # Frontend URL — used for CORS (required in production)
EMAIL_FROM=assessment@yourdomain.com                         # "From" address on all outbound emails
FOUNDER_EMAIL=you@yourdomain.com                             # Founder's email — receives lead notification emails
DASHBOARD_URL=https://your-vercel-app.vercel.app             # Frontend URL used in email links to the dashboard

# Market Data (all free — sign up once)
FINNHUB_API_KEY=your_key_here    # https://finnhub.io → free account → API key (stocks, indices, commodities)
FRED_API_KEY=your_key_here       # https://fred.stlouisfed.org/docs/api/api_key.html → free (Fed rate, CPI, GDP)
GUARDIAN_API_KEY=your_key_here   # https://open-platform.theguardian.com/access → free (5000 req/day news headlines)

# LLM Fallback Chain (Claude → Gemini → Groq)
GEMINI_ASSESSMENT_MODEL=gemini-1.5-flash    # Gemini fallback for assessment calls
GEMINI_OPTIMIZER_MODEL=gemini-1.5-pro       # Gemini fallback for optimizer calls
GROQ_ASSESSMENT_MODEL=llama-3.1-8b-instant  # Groq fallback for assessment calls
GROQ_OPTIMIZER_MODEL=llama-3.3-70b-versatile # Groq fallback for optimizer calls
# GOOGLE_AI_API_KEY already present above — reused for Gemini text generation
# GROQ_API_KEY already present above — reused for Groq text generation

# Client (Vite — must be prefixed VITE_)
VITE_API_URL=http://localhost:3001                           # Base URL of the backend API, used by the React client
