{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node server/dist/src/db/migrate.js && node server/dist/src/db/seeds/runSeed.js && node server/dist/src/index.js",
    "restartPolicyType": "ON_FAILURE"
  },
  "crons": [
    {
      "schedule": "0 3 * * 0",
      "command": "node server/dist/src/scripts/runOptimizer.js"
    },
    {
      "schedule": "0 2 * * *",
      "command": "node server/dist/src/scripts/backfillAnalytics.js"
    }
  ]
}
